install redux
install react-redux
redux-thunk
query-string

react-router-dom
font-awesome
node-sass
prop-types
react-bootstrap

refreshing route - saving the sort order, whole thing rendering the same way including the sort order
link to youtube - other ways would be separate page (would need design) that has video play and list of all the different videos
for now just grabbing the first possible video and playing it via the site provided and if its from a youtube site
